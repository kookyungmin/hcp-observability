apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hcp-api-gateway-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: hcp-api-gateway
  namespaceSelector:
    matchNames:
      - hcp
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
