apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hcp-orchestrator-worker-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: hcp-orchestrator-worker
  namespaceSelector:
    matchNames:
      - hcp
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
