apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hcp-compute-service-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: hcp-compute-service
  namespaceSelector:
    matchNames:
      - hcp
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
